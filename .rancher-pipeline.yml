stages:
- name: Build
  steps:
  - publishImageConfig:
      dockerfilePath: ./Dockerfile
      buildContext: .
      tag: giganet/freepbx
      pushRemote: true
      registry: index.docker.io
timeout: 60
